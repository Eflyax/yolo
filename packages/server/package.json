{
	"name": "@git-yak/server",
	"version": "1.0.0",
	"private": true,
	"module": "src/index.ts",
	"scripts": {
		"dev": "bun run --watch src/index.ts",
		"build": "bun build src/index.ts --compile --outfile dist/server"
	},
	"dependencies": {
		"bun": "^1.3.8"
	},
	"devDependencies": {
		"bun-types": "^1.3.8"
	}
}
